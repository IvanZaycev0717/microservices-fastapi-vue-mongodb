# Скрипт инициализации PostgreSQL

Скрипт инициализации для настройки базы данных PostgreSQL в микросервисной архитектуре.

## Назначение

Автоматическая настройка базы данных при запуске PostgreSQL контейнера. Скрипты выполняются при первом запуске для инициализации окружения.

## Структура базы данных

### 💬 Comments Database (`comments_db`)
**Назначение**: Хранение комментариев и иерархических ответов к проектам

## SQL Скрипт

```sql
SELECT 'CREATE DATABASE comments_db'
WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = 'comments_db')\gexec
```

**Особенности скрипта:**
- Идиоматический подход к созданию БД в PostgreSQL
- Проверка существования базы данных перед созданием
- Использование `\gexec` для выполнения сгенерированной команды
- Предотвращение ошибок при повторном запуске

## Интеграция с микросервисами

База данных `comments_db` используется:
- **Comments Service** - основное хранилище комментариев
- **Admin Service** - модерация и управление комментариями

## Безопасность

- Изолированная база данных для сервиса комментариев
- Настройка пользователей и прав через переменные окружения
- Отдельная схема для данных комментариев

## Примечания

Скрипт выполняется только при первом запуске PostgreSQL. Для переинициализации необходимо удалить volumes и перезапустить контейнеры. Миграции схемы базы данных управляются через Alembic в микросервисе Admin Service.